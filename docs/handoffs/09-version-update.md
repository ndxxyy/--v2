# 09 版本更新：1.1 中药更新框架交接

交接日期：2026-07-12

发布状态：**本地产物已通过构建与 Cloudflare 适配验证；公开 Git 历史清理仍是正式发布硬阻断，尚未推送或上线。**

> 2026-07-14 更新：本文件原第 1–9 节记录 1.1 框架建立时的 113 资产基线。随后同一未提交本地分支依次补齐“解表药·发散风热药”、新增“经方集谱·剂型区别”、替换“九针图谱”水印版，并扩充首页精选轮换。以下 0C 为当前最新补充；0B、0A 与 0 节保留各批次历史，原第 1–9 节保留框架实施历史，不再代表当前数量。

## 0D. 2026-07-16 上线前清理补充

- 已补齐 `robots.txt`、`sitemap.xml`、canonical、三语言 alternate、Open Graph、Twitter 分享信息、manifest 与 favicon；第 7.3 节记录的 SEO 阻断已在当前本地工作树解除。
- 已新建 `docs/handoffs/07-seo-performance.md` 与 `docs/handoffs/08-release.md`；第 7.2 节记录的前置交接缺失已在当前本地工作树解除。
- 已将 `artifacts/**` 与 `content-assets/backups/**` 纳入 Git/ESLint 排除，并将 343 个历史备份文件从当前 Git 索引停止跟踪。本地 137 MB 备份仍完整保留，没有删除或移动。
- 当前已通过类型检查、全量 Lint、274/274 水印验证、Next.js 正式构建、OpenNext Cloudflare 构建、Wrangler dry-run 与本地 Worker 路由/SEO 验证。
- 第 7.1 节仍然有效：向前停止跟踪不会删除 GitHub 旧提交中已公开的受保护备份。必须在取得用户对历史改写和 force-push 的明确授权后单独处理，才能进入正式发布。

## 0C. 当前最新补充：首页精选扩容与每日轮换

### 0C.1 页面与交互

- 首页右侧精选由 3 幅扩为 6 幅：足阳明胃经、桂枝、九针、薄荷、汤剂科普、自煎中药指南。
- 每天按 `Asia/Shanghai` 日期确定稳定首图，同一天内刷新和切换语言不会无序跳图；上一幅、下一幅继续按固定顺序循环，不自动播放。
- 图片始终链接当前可见作品的真实详情页；没有把点击行为改成不可预期的随机详情跳转。
- 新增作品使用完整 `contain` 构图。轮播仍只渲染当前图片，因此扩容没有增加首屏高度，也没有同时请求六张首图。
- 上一幅、下一幅的读屏名称现在指向目标作品；图片链接不再随切换重建，左右方向键切换后焦点保持稳定。

### 0C.2 验证与发布边界

| 验证 | 结果 |
| --- | --- |
| `npm run typecheck` | 通过 |
| `npm run lint` | 通过 |
| `npm run build` | 通过；Next.js 16.2.10，207 个静态页面 |
| 6 幅循环 | 通过；序号、标题、图片、alt、详情链接与前后目标一致 |
| 键盘 | 通过；ArrowRight 切换后焦点仍在作品链接 |
| 1440×900 | 通过；1 个 H1，无横向溢出 |
| 390×844 | 通过；无横向溢出，两个控制按钮均高于 44px |
| 三语言 | 通过；简体、繁体、英文均为 6 幅且无横向溢出 |

- 本批只更新本地；没有提交、推送、PR、Preview、合并、标签、Release 或部署。
- 用户仍需完成最终页面确认；第 7 节既有发布阻断继续有效。

## 0B. 当前最新补充：九针图谱水印版替换

### 0B.1 替换范围

- 用户指定目录实际只有一张完整的 `九针图解.jpg`；本批将其作为“九针图谱”单图原位替换，没有误拆为九项。
- 保持 `work-015`、`XQY-MER-JZ-001`、slug `nine-needles-atlas`、经络分类、图组关系和公开 URL 不变。
- 新图为 3584×4800，SHA-256 为 `5b72f6c48b82ad673e7502d9b320367384dcbe69e10b06a050294e913373e017`，带用户提供的平铺“小钟岐医”网状明水印与右上印章。
- 旧图与新图的低分辨率灰度内容指纹 MSE 为 0.783333，确认是同一九针总览内容的水印替换版。
- 详细来源、旧新哈希和风险记录见 `docs/content-batches/2026-07-13-nine-needles-watermarked-replacement.md`。

### 0B.2 数量、水印与验证

- 母版、作品、图组与公开变体数量均不变：137 张母版、55 件作品、55 个图组、274 张 WebP。
- 只替换 1 张九针母版并重新编码 2 张公开变体；另外 136 张母版与 272 张 WebP 的 SHA-256 均未改变。
- 九针明水印策略由保留既有印章改为继承用户网状明水印；全库记录变为 `applied: true` 238、`applied: false` 36。
- 暗水印稳定指纹仍为 `3e70b7a4`；私有登记表保持 250 条，无密钥漂移、无新冲突、无密钥轮换。

| 验证 | 结果 |
| --- | --- |
| 内容联动 | 通过；55 个 published 图谱作品、55 个图组、137 个资产一一对应 |
| `npm run typecheck` | 通过 |
| `npm run lint` | 通过 |
| `npm run build` | 通过；Next.js 16.2.10，207 个静态页面 |
| 全库水印 | 通过；274/274，最低置信度 0.909 |
| 变更范围 | 通过；只改变九针母版、thumbnail 与 preview |
| 图谱库封面 | 通过；新水印版 956×1280，正常加载 |
| 九针详情页 | 通过；新水印版 1075×1440，编号、标题和 URL 不变 |
| 浏览器状态 | 通过；无横向溢出、无控制台错误 |

### 0B.3 上线边界

- 新图元数据含“大附子”作者归属及“不可商用”类声明和联系方式。联系方式未写入仓库；2026-07-14 用户确认“大附子”就是本人，作者身份冲突已解除，声明继续原样保留。本次确认不等于上线或部署授权。
- 图中文字涉及古籍针具形制、尺寸与用途；正式上线前需医学与编辑复核，页面通用免责声明不能替代内容审核。
- 本批只更新本地；没有提交、推送、PR、Preview、合并、标签、Release 或部署，第 7 节既有发布阻断继续有效。

## 0A. 批次补充：经方集谱·剂型区别

### 0A.1 用户边界与内容

- 用户提供 11 张已经处理明水印的剂型 JPEG，要求加入暗水印并更新本地网页。
- 本批整理为 10 件作品、11 张图片；两张“自煎中药”归为同一双图作品。
- 10 件作品进入“经方集谱”：汤剂、丸剂、散剂、膏剂、丹剂、酒剂、露剂、茶剂、颗粒剂和自煎中药指南。
- 没有创建方剂分类树或新路由。“剂型区别”当前是单一专题，10 件作品未超过既有 18 件渐进显示上限；未来出现第二个专题时再抽象通用专题/系列导航。
- 本批详细来源、逐文件哈希和作品映射见 `docs/content-batches/2026-07-13-formula-dosage-forms.md`。

### 0A.2 图片、水印与数量

| 项目 | 本批前 | 当前本地 | 变化 |
| --- | ---: | ---: | ---: |
| 作品 | 45 | 55 | +10 |
| 经方集谱作品 | 0 | 10 | +10 |
| 母版资产 | 126 | 137 | +11 |
| 经方集谱母版 | 0 | 11 | +11 |
| thumbnail | 126 | 137 | +11 |
| preview | 126 | 137 | +11 |
| WebP 变体 | 252 | 274 | +22 |

- 11 张母版逐字节导入并完成 137 张整库验证；原素材目录未修改。
- 增量派生重新编码 11 张、验证复用 126 张；旧 126 张母版和旧 252 张 WebP 的 SHA-256 全部保持不变。
- 10 张源图继承用户已有网状明水印；一张“自煎中药”仅含印章，准确登记为保留既有印章。没有叠加第二层明水印。
- 暗水印沿用既有密钥与 `xqy-dct-qim-v2`；274/274 通过，最低置信度 0.909，每位最少载体数 7。
- 私有登记表由 239 增至 250 条，只追加本批 11 个 v2 指纹；没有创建、轮换或输出密钥。
- 条件式明水印记录：`applied: true` 236，`applied: false` 38。

### 0A.3 最新验证

| 验证 | 结果 |
| --- | --- |
| 内容联动 | 通过；55 个 published 图谱作品、55 个图组、137 个资产一一对应 |
| `npm run typecheck` | 通过 |
| `npm run lint` | 通过 |
| `npm run build` | 通过；Next.js 16.2.10，207 个静态页面 |
| 全库水印 | 通过；274/274，最低置信度 0.909 |
| 旧资产不变 | 通过；126 张旧母版与 252 张旧 WebP 的 SHA-256 均未改变 |
| 简体页面 | 通过；经方集谱 10 件作品、10 张封面均加载 |
| 繁体与英文页面 | 通过；各 10 件作品、10 张封面均加载 |
| 双图详情 | 通过；“自煎中药指南”显示 2 张并可切换至第 2 张 |
| 浏览器状态 | 通过；无横向溢出、无控制台错误 |

### 0A.4 新增上线阻断

- 11 张源图元数据均含“大附子”作者归属及“不可商用”类版权声明和联系方式。联系方式未写入仓库；2026-07-14 用户确认“大附子”就是本人，作者身份冲突已解除，声明继续原样保留。本次确认不等于上线或部署授权。
- 图片包含剂型、煎煮、服用、功效和安全类文字；页面通用免责声明不能替代逐图医学与编辑复核。
- 本批只在本地展示；没有提交、推送、PR、Preview、合并、标签、Release 或部署。第 7 节既有发布阻断继续有效。

## 0. 批次补充：解表药·发散风热药

### 0.1 用户边界

- 用户提供 13 张“第二节 发散风热药”水印 JPEG，并要求在“本草金鉴”下建立“解表药”。
- 用户确认原图明水印已经完成，要求本地派生图继续加入暗水印。
- 本轮只修改本地；没有推送、PR、Preview、合并、标签、Release 或部署。
- 13 味是用户一次提供的完整类别，因此作为一个 13 味特批批次导入。详细记录见 `docs/content-batches/2026-07-13-herbs-wind-heat.md`。

### 0.2 页面与内容

- 新增可递归本草分类树：`本草金鉴 → 解表药 → 发散风寒药 / 发散风热药`。
- 新增三语言静态分类路径：
  - `/{locale}/atlas/herbs/exterior-releasing`
  - `/{locale}/atlas/herbs/exterior-releasing/wind-cold`
  - `/{locale}/atlas/herbs/exterior-releasing/wind-heat`
- 既有 17 味本草显式归入发散风寒药，ID、slug、编号及详情 URL 均未变化。
- 新增 13 味发散风热药：薄荷、牛蒡子、蝉蜕、桑叶、菊花、蔓荆子、柴胡、升麻、葛根、淡豆豉、浮萍、木贼、谷精草。
- 新作品使用 `work-118` 至 `work-130` 的内部 ID；因用户未提供访客编号，页面不显示也不猜测 `XQY-HERB-WH-*` 编号。
- 无编号作品进入授权/联系页时带入本地化标题而非内部 ID；搜索索引加入稳定 slug 词元，支持英文 URL 名称检索。
- 新增分类页面包屑、父子分类切换、完整分类路径、详情回链；同类推荐优先使用相同叶分类。
- 分类导航按“本草总目录 / 所在章 / 本章分节”显式分层，用“第一章、第一节”等序号区分级别，不再把父级与子级平铺。
- 总目录桌面端双列、手机端单列；作品列表首批显示 18 件，其余作品逐批展开，避免未来 21 章、数百味药在同一页面无限拉长。
- 页面文字没有从图片转录或推断性味、归经、功效、主治、剂量等医学字段。三语言显示文字待用户上线前最终确认。

### 0.3 图片与水印

| 项目 | 框架基线 | 当前本地 | 变化 |
| --- | ---: | ---: | ---: |
| 作品 | 32 | 45 | +13 |
| 本草作品 | 17 | 30 | +13 |
| 母版资产 | 113 | 126 | +13 |
| 本草母版 | 17 | 30 | +13 |
| thumbnail | 113 | 126 | +13 |
| preview | 113 | 126 | +13 |
| WebP 变体 | 226 | 252 | +26 |

- 13 张来图均为 3584×4800 JPEG，SHA-256 唯一；`.DS_Store` 已排除，用户原文件夹未修改。
- 受保护母版已由小批次导入器逐字节导入并完成 126 张整库验证。
- 增量派生重新编码 13 张、验证复用 113 张；随后无变化运行重新编码 0、验证复用 126 张。
- 明水印只继承用户已有平铺标识，没有叠加第二层。
- 暗水印沿用 `xqy-dct-qim-v2` 与既有密钥；252/252 通过，最低置信度 0.909，每位最少载体数 7。
- 私有登记表保持追加式：v1 113 + v2 126 = 239 条；没有输出、删除或改写指纹与密钥。
- 条件式明水印记录：`applied: true` 216，`applied: false` 36。

### 0.4 该本草批次验证

| 验证 | 结果 |
| --- | --- |
| 内容联动 | 通过；45 个 published 图谱作品、45 个图组、126 个资产一一对应 |
| `npm run typecheck` | 通过 |
| `npm run lint` | 通过 |
| `npm run build` | 通过；177 个静态/动态页面入口，含三语言本草分类路径 |
| 无变化图片生成 | 通过；0 重新编码、126 复用 |
| 全库水印 | 通过；252/252 |
| 本地 HTTP | 三语言分类页、薄荷详情、既有苍耳子详情与图片均为 200；未知本草分类为 404 |
| 访客编号 | 薄荷详情不含猜测的 `XQY-HERB-WH-*` |
| 授权/联系关联 | 薄荷使用“薄荷图谱”作为关联值，不使用 `work-118`；联系页显示中性“关联作品”文案 |
| 搜索索引 | 薄荷搜索文本包含 `mint-herb-atlas` 稳定词元；固定分类初始页不渲染无效“清除搜索”按钮 |
| 浏览器层级验收 | 1440×900 与 390×844 均通过；无横向溢出，本草金鉴、所在章、当前节选中状态可读 |
| 长列表交互 | 本草总目录首批 18/30 件；“继续显示 12 件”已实测展开到 30/30 |

已在真实内置浏览器执行桌面与手机视图验收；用户仍需在上线前完成主观视觉确认，并且仍须先处理本文件第 7 节的历史母版、SEO 与前置交接阻断。

## 1. 基线与分支

- 工作分支：`content/herbs-v1.1`
- 基线 `main`：`78106b69a443783b3ab48169cdc0bf39a9617750`
- 远程 GitHub `main` 只读核对结果：同为 `78106b69a443783b3ab48169cdc0bf39a9617750`
- 正式 URL：`https://xiao-zhong-qi-yi-v2.zhongzhaoyuan165.workers.dev/zh-CN`
- `v1.0.0` 标签：本地不存在；GitHub API 返回 404，远程也不存在。本任务没有创建标签或 Release。
- 任务开始时工作区干净；从已核对的 `main` 创建本地分支后才开始写文件。
- 启动提示文件只存在于本地分支 `docs/09-version-update-prompt` 的提交 `c13bb2f`，不在当前 `main` 或本工作分支；任务已按用户指定内容执行，本次没有越权把该文档并入 09 分支。

## 2. 完成内容

### 2.1 版本化资产审核清单

- 新增 Git 跟踪的 `scripts/atlas/asset-manifest.json`，作为公开生成允许列表和数量唯一事实来源。
- 清单逐资产登记：稳定资产 ID、母版相对路径、所属作品 ID、内容类别、来源类型、源 SHA-256、来源审核、公开批准、明水印策略、明水印来源、公开变体和审核证据引用。
- 清单 schema 采用严格版本匹配和精确字段校验；未知版本、未知字段、未批准状态、重复 ID/路径/哈希、路径冲突、来源与水印策略冲突均拒绝。
- 当前显式资产为 113 个；变体角色当前严格限定为 `thumbnail` 与 `preview`，共 226 个。未来新增角色必须先同步扩展盲水印、生成类型与验证器，不能只改 JSON。
- 版本化清单与 `src/data/works.ts`、`src/data/image-groups.ts` 做 AST 级交叉验证，覆盖作品 ID、slug、编号、类别、显式发布状态、三语言标题/简介、图组和资产归属。

### 2.2 母版导入与公开图生成

- 私有 `.atlas-visible-masters.json` 只作为导入证据；实际母版、私有证据和版本化清单按 ID、路径、哈希、尺寸、来源和明水印描述逐项交叉验证。
- 母版目录拒绝未知文件、缺图、符号链接、特殊文件和 realpath 越界。
- `generate-visible-masters.mjs` 保留 1.0 全量经络导入，同时新增 `--asset` 小批次模式：
  - 单资产可用 `--incoming-file`；
  - 多资产必须使用与清单相对路径完全一致的受控 `--incoming` 目录；
  - 未选母版与私有证据先逐项验证再复制；
  - 暂存整库通过验证后才原子替换受保护母版目录。
- `generate-images.mjs` 支持 `--asset` 单味/小批次生成。未变化资产只在源哈希、变体规格/编码契约、路径、生成元数据和盲水印全部一致时复用；规格契约变化会强制全量重生成，显式不完整批次会失败关闭。
- 公开目录与 generated 清单继续使用暂存、备份、异常回滚和原子 rename；发现上次事务残留时停止并给出恢复提示，不继续覆盖。
- 本次全量迁移没有改变任何公开 WebP 字节；generated 清单新增版本、编码契约指纹和母版源 SHA-256。

### 2.3 水印与登记表

- 明水印策略数量从版本化清单计算，不再由脚本硬编码 `95/18/113` 或 `190/36/226`。
- 所有公开变体继续使用 `xqy-dct-qim-v2`；生成和复用时均解码提取并核对品牌、角色和指纹。
- `.atlas-watermark-registry.json` 缺失、空表、格式错误、重复/冲突历史、当前指纹缺失或同一方案/资产指纹漂移时均失败关闭。
- 登记表保持追加式；本次仍为历史 v1 113 条 + 当前 v2 113 条，共 226 条，没有删除或改写。
- 生成器在公开资源替换前预登记新指纹，以保证任何成功公开的资产一定可归因；若后续公开事务失败，追加记录作为未发布历史保留，不回删登记表。该行为不等于内容已发布。

### 2.4 内容和治理

- `src/data/works.ts` 不再由 helper 自动把新增记录设为 `published`。每件作品必须显式声明 `publication`；现有 32 件基线作品保持显式 `published`。
- 新增 `docs/herb-update-template.md`，覆盖 1–5 味小批次、三语言独立审核、医学来源、版权/公开许可、母版、明/盲水印、数量变化、负向测试和三级权限闸门。
- 更新 `docs/work-ownership.md`，明确任务 09 的文件边界、04→09 顺序接管和批次串行规则。
- 新建 `CHANGELOG.md`，按新增内容、内容修订、图片变更、技术变更和已知限制记录 1.1。
- ESLint 明确忽略 `.next/**`、`.open-next/**`、`.agents/**`、`node_modules/**` 和受保护母版。

## 3. 修改文件

### 版本与文档

- `CHANGELOG.md`
- `docs/herb-update-template.md`
- `docs/work-ownership.md`
- `docs/handoffs/09-version-update.md`
- `eslint.config.mjs`

### 内容数据

- `src/data/works.ts`
- `src/data/atlas-image-assets.generated.ts`（由脚本生成）

`src/data/image-groups.ts` 本批次只读取校验，没有修改；本框架没有新增作品或图片关系。

### 资产、生成与水印脚本

- `scripts/atlas/asset-manifest.json`
- `scripts/atlas/asset-manifest.mjs`
- `scripts/atlas/master-library.mjs`
- `scripts/atlas/validate-content-links.mjs`
- `scripts/atlas/generate-images.mjs`
- `scripts/atlas/generate-visible-masters.mjs`
- `scripts/atlas/verify-watermarks.mjs`
- `scripts/atlas/visible-watermark.mjs`
- `scripts/atlas/visible-source-baselines.mjs`
- `scripts/atlas/watermark-registry.mjs`

没有修改 `wrangler.jsonc`、Cloudflare Worker 名称、域名、生产配置、旧网站、临证笔记、联系、作者、授权数据或页面视觉布局。

## 4. 内容与数量变化

本框架阶段没有新增、替换或修订中药内容；没有生成新编号，也没有改变首页精选。

| 项目 | 更新前 | 更新后 | 变化 |
| --- | ---: | ---: | ---: |
| 公开作品 | 32 | 32 | 0 |
| 公开本草作品 | 17 | 17 | 0 |
| 经络/针具作品 | 15 | 15 | 0 |
| 图组 | 32 | 32 | 0 |
| 母版资产 | 113 | 113 | 0 |
| 经络/针具母版资产 | 96 | 96 | 0 |
| 本草母版资产 | 17 | 17 | 0 |
| 公开 `thumbnail` | 113 | 113 | 0 |
| 公开 `preview` | 113 | 113 | 0 |
| 公开 WebP 变体 | 226 | 226 | 0 |

新增或修改的作品 ID、slug、编号：**无**。

三语言审核状态：本框架没有新增公开文字；现有 32 件 published 作品均通过三语言标题和简介非空校验。

## 5. 水印结果

| 项目 | 结果 |
| --- | ---: |
| `provided-mesh` 资产 | 95 |
| `preserve-existing-mesh` 资产 | 17 |
| `preserve-existing-seal` 资产 | 1 |
| 明水印 `applied: true` 公开变体 | 190 |
| 明水印 `applied: false` 公开变体 | 36 |
| `xqy-dct-qim-v2` 通过 | 226 / 226 |
| 最低盲水印置信度 | 0.909 |
| 每位最少载体数 | 7 |
| 私有登记表历史 | v1 113 + v2 113 = 226 |

密钥、登记表和正式私有导入证据仍为 `0600`，已被 Git 忽略；本次没有创建、轮换、提交或输出密钥。

## 6. 验证结果

| 验证 | 结果 |
| --- | --- |
| `npm run typecheck` | 通过，0 错误 |
| `npm run lint` | 通过，0 错误/警告 |
| `npm run build` | 通过；Next.js 16.2.10，129 个页面入口 |
| `npm run atlas:images -- --source content-assets/masters/works-watermarked` | 通过；重新编码 0，验证复用 113；公开 WebP 无 Git 差异 |
| `npm run atlas:watermark:verify` | 通过；226/226，最低置信度 0.909 |
| 版本化清单统计 | 通过；113 资产、226 变体 |
| 内容联动 | 通过；32 published 作品、32 图组、113 资产一一对应 |
| 单资产增量 | 通过；实测重新编码 1、验证复用 112，随后全库 226/226 |
| 母版小批次演练 | 通过；单文件与严格目录两种临时整库演练，正式母版未修改 |
| 负向测试 | 未知 schema、未批准公开、重复 ID/变体、策略冲突、未知资产、哈希不符、symlink、路径越界、registry 缺失、历史冲突和密钥漂移均正确拒绝 |
| `git diff --check` | 通过 |

本地 HTTP 检查：

- `/zh-CN/atlas/herbs`：200
- `/zh-Hant/atlas/herbs`：200
- `/en/atlas/herbs`：200
- `/zh-CN`：200
- 三语言代表详情 `xanthium-herb-atlas`：均为 200
- 不存在的详情：404
- 代表本草缩略图：200
- `/sitemap.xml`：404
- `/robots.txt`：404

浏览器视觉检查：已使用真实内置浏览器在 1440×900 与 390×844 执行复核；两个视图的 `scrollWidth` 均不超过可见宽度，未发现横向滚动。面包屑、章/节层级、当前分类选中态与作品分批显示均已验证。

正式 URL 检查：当前执行环境的只读请求连续超时，无法在本轮确认正式站可访问；没有把该前置条件伪写成通过。

## 7. 发布阻断与遗留问题

### 7.1 已跟踪的受保护备份（P1，发布硬阻断）

`main` 自初始提交 `cb98405` 起已跟踪：

`content-assets/backups/20260711-before-user-provided-meridians/**`

只读核对结果：343 个 Git 文件、142,738,427 字节，其中含 113 张全分辨率 JPG、226 张旧公开 WebP、私有 `.atlas-visible-masters.json` 和一份旧 generated 清单。该目录不含盲水印密钥或 `.atlas-watermark-registry.json`，但已经违反“母版和私有证据不得提交 Git”的边界。

本任务不拥有该历史备份或 `.gitignore`，没有删除、移动、停止跟踪或重写历史。GitHub API 可匿名读取仓库 `main`，应按已经公开暴露处理。发布前必须另开安全任务并取得用户明确授权，至少：

1. 先确认仓库外安全备份；
2. 前向停止跟踪并补充 ignore/提交守卫；
3. 因仓库可匿名访问，评估并执行 Git 历史清理、协作者重克隆和 GitHub 缓存/引用处置。

当前没有证据表明盲水印密钥进入 Git，因此本轮不建议擅自轮换；若未来发现密钥泄露，旧密钥仍需离线保留以验证历史水印。

### 7.2 前置交接缺失

启动提示声明 08 已完成并要求读取 `docs/handoffs/08-release.md`，但 `main` 和当前分支均不存在该文件；旧 `docs/HANDOFF.md` 反而记录 08 尚未完成。本任务没有越权虚构或补写 08 交接。

### 7.3 SEO 路由缺失

`main` 不含 `src/app/sitemap.ts` 和 `src/app/robots.ts`；本地 `/sitemap.xml`、`/robots.txt` 均为 404，与启动提示所述 07/08 完成状态冲突。任务 09 不拥有这些文件，本次没有越界补建；发布前需作为独立基线修复处理。

### 7.4 尚未完成的人工/环境验证

- 用户对当前本地页面的最终主观视觉确认。
- 从当前执行环境访问正式 URL 的可用性确认。
- 仓库外盲水印密钥与登记表安全备份的人工确认。

## 8. 后续任务注意事项

1. 在处理 7.1 的受保护备份之前，不推送本分支、不创建 PR、Preview Version 或正式发布。
2. 安全清理、08 交接/SEO 基线修复完成后，应将本分支变基或重建到新的安全 `main`，再重跑全部验证。
3. 后续每批 1–5 味中药使用独立分支；先填写 `docs/herb-update-template.md`，再更新清单、作品、图组和生成资源。
4. 新资产未完成三语言、医学来源、版权、公开许可和明水印审核时保持 `draft`，不得进入版本化公开清单。
5. 当前只支持 `thumbnail` 与 `preview`；新增 `social` 或其他角色必须先端到端扩展盲水印、generated 类型、生成器和验证器。
6. 沿用现有密钥和追加式登记表；不得删除历史指纹，不得为新批次新建密钥。
7. 收到用户明确指令“创建预览，不上线”后，也必须先解除本交接的发布阻断，再考虑非生产 Preview；当前指令没有开放该权限。

## 9. 最终状态

- 当前分支：`content/herbs-v1.1`
- 当前本地提交：`Prepare v1.1 release`
- 推送：远程存在同名基线分支，本地发布提交尚未推送
- PR：无
- Cloudflare Preview：只完成本地 Worker 预览验证，未创建远程 Preview Version
- 合并 `main`：无
- 标签 / Release：无
- 正式部署：无
- 正式 Worker 配置：本地部署包已更新，远程 Worker 未改动
- 正式网站：本轮执行环境连接已知 Workers 域名超时，未将可达性伪写为通过
- 发布判断：**代码与本地部署产物已通过；必须先处理公开 Git 历史中的受保护备份，并确认正式主域名，才可推送与正式上线。**
