# 数据目录

任务 01 保持所有集合为空。不得从旧站批量复制或以示例数据填充。

- `works.ts`、`image-groups.ts`、`work-categories.ts`：任务 04 复核并迁移。
- `cases.ts`、`case-categories.ts`：任务 05 管理；没有真实医案时继续为空。
- `license-scenarios.ts`、`contacts.ts`、`author.ts`：任务 06 在真实资料复核后填写。

公开页面必须通过选择器过滤发布状态，不能直接导入内部审核字段。

