# 小钟岐医 V2 变更记录

本文件只记录已经发生且可核对的版本变化。尚未完成审核、生成或验证的事项必须明确标为“待验证”，不得写成已完成；版权证明、医学审校记录、母版、密钥及其他私密证据不得写入本文件。

每个中药小批次固定按以下类别记录：

- 新增内容
- 内容修订
- 图片变更
- 技术变更
- 已知限制

批次合入正式版本前，还必须记录数量变化、三语言审核、水印验证和发布状态。详细逐味资料使用 `docs/herb-update-template.md`，完整验证证据写入 `docs/handoffs/09-version-update.md`。

## 未发布补充：1.1 首页精选作品扩容与每日轮换

发布状态：**仅本地，尚未上线**。

- 首页右侧精选由 3 幅扩为 6 幅，当前纳入足阳明胃经、桂枝、九针、薄荷、汤剂科普和自煎中药指南，图片数量增加但页面高度不增加。
- 每天按北京时间确定一幅稳定首图；同一天刷新与三语言页面保持一致。上一幅、下一幅仍按固定顺序循环，不自动播放，也不把图片链接随机指向其他作品。
- 新增图统一完整 `contain` 展示；当前作品图片继续进入对应详情页。
- 修正上一幅、下一幅读屏名称，使其说明目标作品；切换作品时不再重建图片链接，键盘左右切换后焦点保持在链接上。
- 类型检查、完整 Lint、正式本地构建和桌面/手机三语言浏览器检查均通过；构建仍生成 207 个静态页面。
- 本批没有提交、推送、PR、Cloudflare Preview、合并、标签、Release 或部署。

## 未发布补充：1.1 九针图谱水印版替换

发布状态：**仅本地，尚未上线**。

### 图片变更

- 用用户指定目录中的单张 `九针图解.jpg` 原位替换 `nine-needles-atlas`；“九针”是作品名称，没有拆成九个文件。
- `work-015`、作品编号 `XQY-MER-JZ-001`、slug、分类、图组与公开图片 URL 全部不变。
- 新母版为 3584×4800，带平铺“小钟岐医”网状明水印和右上印章；不再叠加第二层明水印。
- 只重新编码九针的 thumbnail 与 preview；另外 136 张母版、272 张公开 WebP 的 SHA-256 全部不变。
- 暗水印继续使用稳定指纹 `3e70b7a4`；全库 274/274 通过，明水印记录更新为 `applied: true` 238、`applied: false` 36。

### 验证与限制

- 内容联动、类型检查、完整 Lint、正式本地构建和浏览器页面检查均通过；构建仍为 207 个静态页面。
- 新图元数据含“大附子”作者归属及“不可商用”类版权声明；2026-07-14 用户确认“大附子”就是本人，作者身份冲突已解除，声明继续作为作者对外使用限制原样保留。
- 图中文字涉及古籍针具形制、尺寸与用途，正式上线前仍需医学与编辑复核，不能作为自行针刺操作指导。
- 本批没有提交、推送、PR、Cloudflare Preview、合并、标签、Release 或部署。

## 未发布补充：1.1 经方集谱“剂型区别”本地批次

发布状态：**仅本地，尚未上线**。

### 新增内容

- 新增汤剂、丸剂、散剂、膏剂、丹剂、酒剂、露剂、茶剂、颗粒剂 9 件科普图谱，以及 1 件含两张配套图的“自煎中药指南”。
- 10 件作品进入“经方集谱”，三语言标题和保守目录性简介完整；没有猜测访客作品编号。
- “剂型区别”暂作为内容系列写入资产路径与简介。当前只有一个专题、10 件作品，继续使用既有经方集谱页；出现第二个明确专题时再抽象通用专题/系列导航。

### 图片变更

- 从用户指定的仓库外目录逐字节导入 11 张已有明水印的 JPEG；原目录和原文件均未修改。
- 新增 11 张受保护母版、11 张缩略图和 11 张预览图；原有 126 张母版与 252 张 WebP 的 SHA-256 全部保持不变。
- 10 张继承用户已有平铺网状明水印，1 张保留“小钟岐医”印章；所有 22 张公开变体均嵌入 `xqy-dct-qim-v2` 暗水印，没有再叠加明水印。

### 数量变化

| 项目 | 本批前 | 当前本地 | 变化 |
| --- | ---: | ---: | ---: |
| 本地目录作品 | 45 | 55 | +10 |
| 经方集谱作品 | 0 | 10 | +10 |
| 全分辨率母版 | 126 | 137 | +11 |
| 经方集谱母版 | 0 | 11 | +11 |
| 本地 WebP 变体 | 252 | 274 | +22 |

### 审核与验证状态

- 内容联动通过：137 个资产、55 个图组、55 件 published 图谱作品一一对应。
- 三语言经方集谱页面均显示 10 件作品；双图详情切换正常，图片加载正常，无浏览器控制台错误。
- 全库暗水印 274/274 通过，最低置信度 0.909；条件式明水印记录为 `applied: true` 236、`applied: false` 38。
- 类型检查、完整 Lint 与正式本地构建均通过；构建生成 207 个静态页面。

### 已知限制

- 11 张源图的元数据均带“大附子”作者归属及“不可商用”类版权声明；2026-07-14 用户确认“大附子”就是本人，作者身份冲突已解除，声明继续作为作者对外使用限制原样保留。
- 图片含煎煮、服用、功效与安全类文字；本地页面可供用户核对，但正式上线前仍需医学与编辑复核。
- 资产清单的本地公开批准只用于生成本地 `public` 派生图，不能替代用户的部署确认。
- 本批没有推送、PR、Cloudflare Preview、合并、标签、Release 或部署；任务 09 的历史母版、SEO 和前置交接发布阻断仍然有效。

## 未发布：1.1 解表药分类与发散风热药本地批次

发布状态：**仅本地，尚未上线**。

- 当前工作分支：`content/herbs-v1.1`
- 本批次范围：在 1.1 框架内建立本草递归分类，并一次性补齐用户提供的 13 味发散风热药
- 远程状态：未推送、未创建 PR、未创建 Cloudflare Preview Version
- 生产状态：未合并 `main`、未创建正式 Release、未部署正式 Worker

### 新增内容

- 新增“本草金鉴 → 解表药 → 发散风寒药 / 发散风热药”分类树与三语言分类路径。
- 新增薄荷、牛蒡子、蝉蜕、桑叶、菊花、蔓荆子、柴胡、升麻、葛根、淡豆豉、浮萍、木贼、谷精草 13 件本地图谱作品。
- 新作品使用稳定内部 ID 与 URL slug；用户未提供访客作品编号，因此没有猜测或显示 `XQY-HERB-WH-*` 编号。

### 内容修订

- 现有 17 味本草显式归入“解表药 → 发散风寒药”，作品 ID、slug、编号和详情 URL 均未改变。
- 新增作品只使用文件名、用户给定分类和保守的目录说明；没有从图片抄录性味、归经、功效、主治或剂量等医学字段。

### 图片变更

- 从用户指定的仓库外目录逐字节导入 13 张 3584×4800 水印 JPEG；原目录和原文件名未修改，`.DS_Store` 已排除。
- 受保护母版新增 `herbs/wind-heat/**` 13 张；由脚本新增 13 张缩略图与 13 张预览图，未重新编码其余 113 张资产。
- 新图沿用原有“小钟岐医”平铺明水印，不重复叠加；全部公开变体继续嵌入 `xqy-dct-qim-v2` 盲水印。

### 技术变更

- 新增 `src/data/herb-taxa.ts` 与递归本草分类选择器；新增 `/{locale}/atlas/herbs/[...taxonomy]` 静态分类路由、面包屑、分类切换和详情回链。
- 本草目录改为“按章浏览 → 本章分节 → 分节作品”三层结构；当前页明确标出“所在章”与“第一节 / 第二节”，父级与子级不再平铺为同级项。
- 本草总目录在桌面端使用紧凑双列章节索引，手机端回落单列；作品列表首批显示 18 件，余下作品通过真实可用的“继续显示”按钮逐批展开。
- `Work` 新增 `herbTaxonKey`；本草作品必须显式绑定分类，未确认的访客编号允许省略，非本草作品禁止误填本草分类。
- 内容联动验证新增本草分类与资产路径一致性检查；`herbs/wind-cold/**`、`herbs/wind-heat/**` 与作品分类不一致时失败关闭。
- 无访客编号的新作品在授权/联系入口中带入本地化作品标题，不再把内部 `work-*` ID 当作作品编号；稳定 slug 同时加入搜索索引，英文可按 `mint`、`arctium` 等 URL 词元检索。
- 新增 `docs/herb-update-template.md`，统一小批次资料录入、三语言审核、版权与医学来源、公开许可、明水印和盲水印检查、数量变化及发布闸门。
- 更新 `docs/work-ownership.md`，将任务 09 放在任务 08 之后，并约束后续中药批次顺序处理。
- 将 `src/data/works.ts` 的发布状态改为逐作品显式声明；现有 32 件基线作品保持 `published`，后续新增作品遗漏 `publication` 时会在类型检查阶段失败，不再由 helper 自动公开。
- 版本化显式资产清单现登记 126 个母版、252 个 `thumbnail` / `preview` 变体及逐资产来源哈希、公开批准、作品绑定和明水印策略；数量不再由脚本硬编码。
- 图片与母版生成器新增 `--asset` 小批次模式、未变化资产校验复用、整库暂存与异常回滚；本批次重新编码 13、复用 113，最终无变化运行重新编码 0、复用 126。
- 新增编码契约指纹，绑定尺寸、质量、格式、缩放/色彩/WebP 参数和盲水印方案；规格变化时部分批次失败关闭并要求全量重生成。
- 加强私有登记表、符号链接/路径越界、未知文件、内容数据绑定和事务残留检查；登记表历史为 v1 113 + v2 126 = 239，没有删除或改写。

### 数量变化

本次按用户“解表药已经完整”的明确范围，将 13 味发散风热药作为一个完整类别特批处理，没有拆成 1–5 味小批次。本表仍不能替代交接中的完整命令结果。

| 项目 | 1.0 基线 | 该本草批次完成时 | 变化 |
| --- | ---: | ---: | ---: |
| 本地目录作品 | 32 | 45 | +13 |
| 本地本草作品 | 17 | 30 | +13 |
| 全分辨率母版 | 113 | 126 | +13 |
| 严格经络母版 | 95 | 95 | 0 |
| 本草母版 | 17 | 30 | +13 |
| 九针母版 | 1 | 1 | 0 |
| 本地 WebP 变体 | 226 | 252 | +26 |

### 审核与验证状态

- 三语言：新增简体与繁体标题，英文题名使用拼音；简介只描述目录归属。它们用于本地确认，正式上线前仍等待用户对三语言显示文字作最终确认。
- 版权与医学来源：图片来自用户明确指定的“版权与水印资质”目录；本批次没有把图中文字转录为结构化医学资料，正式上线前仍需按用户最终确认处理。
- 公开许可：用户本轮只授权更新本地网页，未授权推送、预览或正式上线；清单的批准状态只用于生成本地 `public` 派生图，不能替代部署确认。
- 明水印：13 张来图均按用户提供的平铺“小钟岐医”水印策略继承；全库记录为 `applied: true` 216 张、`applied: false` 36 张。
- 盲水印：`xqy-dct-qim-v2` 全库验证通过 252/252，最低置信度 0.909。
- 本地质量闸门：内容联动、类型检查、完整 Lint、差异检查与全库水印验证已通过；正式构建和页面复核结果见 `docs/handoffs/09-version-update.md`。

### 已知限制

- 13 件新作品暂不显示访客编号；待用户确认编号体系后再单独补充，不能从现有 `WC` 编号推算。
- 本地 `publication.status` 与资产批准用于让用户查看完整页面，不代表已经获得推送、Preview 或正式部署权限。
- `main` 已从历史提交中跟踪 `content-assets/backups/20260711-before-user-provided-meridians/**`，其中包含 113 张全分辨率 JPG 和私有导入清单；该目录不含盲水印密钥或登记表，但仍违反受保护母版不入 Git 的边界。任务 09 不拥有该历史备份，本次未删除、移动或重写历史；在单独获得用户授权并完成安全处置前，1.1 正式发布保持阻断。
- 当前分支缺少启动提示中列出的 `docs/handoffs/08-release.md`。这是已知的历史文档缺口；任务 09 不拥有该文件，本次没有创建或修改 08 交接，正式发布前需由有权限的任务确认处理方式。
- 当前 `main` 不含 `src/app/sitemap.ts` 和 `src/app/robots.ts`；本地 `/sitemap.xml` 与 `/robots.txt` 均返回 404，与启动提示所述任务 07/08 完成状态不一致。任务 09 不拥有这些 SEO 路由，本次没有越界补建；正式发布前需作为独立基线修复处理。
- 已在内置浏览器完成 1440×900 与 390×844 分类页视觉复核；两个宽度均无横向溢出，父子层级、当前节选中状态和章节序号可读。作品分批显示已实际从 18 件展开到 30 件；正式上线前仍等待用户的主观页面确认。
- 本地与远程在任务开始时均未发现 `v1.0.0` 标签；仅建议在用户确认后补建，当前没有创建本地或远程标签，也没有创建 Release。
- 未提供真实编号、三语言审校、版权/医学来源、公开许可或可确认明水印策略的中药，不能公开。
- 盲水印不能替代版权证明，也不保证抵抗强缩放、大幅裁切、极端压缩、AI 重绘或打印再拍。

## 1.0 线上基线（记录于 2026-07-12）

- 正式基线分支：`main`
- 任务 09 开始时的本地 `main` 提交：`78106b69a443783b3ab48169cdc0bf39a9617750`
- 正式 URL：`https://xiao-zhong-qi-yi-v2.zhongzhaoyuan165.workers.dev/zh-CN`
- 已验收内容：32 件公开作品，其中 17 件本草作品
- 已验收图片：113 张全分辨率母版、226 张公开 WebP 变体
- 已验收水印：226/226 张公开变体通过 `xqy-dct-qim-v2`；明水印记录为 `applied: true` 190、`applied: false` 36

以上为任务 09 的回归基线，不代表本地 1.1 工作已经发布。任何后续批次都必须在自己的变更记录中填写真实前后数量，不得复制基线数字冒充本批次验证。
